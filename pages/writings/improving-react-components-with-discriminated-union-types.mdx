import { WritingLayout } from '@/components/index';

export const meta = {
  title: 'Improving react components with discrimative union types',
  summary: 'How to build strict react components with typescripts discrimited union types',
  date: '31 December, 2021',
  published: false,
};

Let's build a strict react component with typescript. The component in question is

export default function ({ children }) {
  return <WritingLayout meta={meta}>{children}</WritingLayout>;
}
