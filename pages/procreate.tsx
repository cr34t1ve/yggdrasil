import type { NextPage } from 'next';
import { Navbar } from '@/components/index';

const ProcreatePage: NextPage<{}> = ({}) => {
  return (
    <div className="container">
      <Navbar />
    </div>
  );
};

export default ProcreatePage;
